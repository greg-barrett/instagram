<%= form_for(@user.passive_followings.find_by(follower_id: current_user.id), remote: true, html: { method: :delete }) do |f| %>
<%= f.submit "Following", class: :follow_btn, id: :follow_btn %>
<% end %>
