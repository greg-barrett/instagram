
 <%= form_for @post.likes.build, remote: true do |f| %>
 <%= f.hidden_field :post_id, value: @post.id %>
 <%= f.submit "Like", :type => :image, :src => image_path("like.png"), :class=> [:like, :no_val] %>
 <% end %>
